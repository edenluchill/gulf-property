# ⚠️ **一旦有了这个文件,Cloudflare Pages 就不再自动做 SPA 兜底** ——
#    所有的路由规则都得自己写全,包括那条 catch-all。
#    (我第一版只写了 /assets/* 那条,结果 /v/:code、/factsheet/:code 全 404 —— 线上炸了。
#     UI 冒烟两分钟就抓到了。)

# 🔴 找不到的静态资源必须 404 —— 不能拿 index.html 顶包。
#
# 默认的 SPA 兜底会把**所有找不到的路径**都回一份 index.html(200)。于是当浏览器去取
# 一个**上一次部署的** chunk 时(标签页开着没刷新、按了 back):
#     /assets/index-OLDHASH.js  →  200 + text/html
#     → "Failed to load module script: expected JavaScript but got MIME type text/html"
#     → **整页白屏**
#
# 静态资源找不到就该 404。这样浏览器会正常抛「模块加载失败」,
# 前端的兜底(main.tsx 里的 vite:preloadError → 强刷一次)才能接住它。
/assets/*  /404.html  404

# 其余一切交给 SPA(必须放在最后)
/*  /index.html  200
